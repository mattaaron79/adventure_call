---
id: tic-1250
title: 'Hover the nearest relationship line: highlight it and double-click to fly
  to its target'
status: closed
type: feature
tier: medium
domain: ui
epic: viz-modes-next
priority: null
tags:
- canvas
- hover
- edges
- interaction
assignee: deepseek.v4-flash.001
depends_on: []
blocked_by: null
created: '2026-09-02T11:12:02'
updated: '2026-09-02T11:53:16'
closed: '2026-09-02T11:53:16'
---

## Description
Wishlist item, captured from the request: "When hovering over a relationship line or group of relationship lines, visually indicate the nearest line under the cursor. Double clicking should teleport the view to the target of that line."
Addendum (from Matt): With the popup that displays the info for lines under the cursor, bold the top line to indicate the closest line.

Analysis: most of the machinery already exists, so this is wiring rather than new infrastructure. `edgesNearPoint(scene, worldPoint, radius, limit)` in web/src/canvas/scene.ts (shipped for the near-pointer connection summary, tic-f1d7) already finds the edges within a constant screen-pixel radius of the pointer, nearest first, over the culled visible scene. Existing highlight plumbing (`connectionEdgesIncidentTo`, `highlightedEdgesLast`, the accent colours in web/src/canvas/theme.ts) shows how a lit line is drawn and ordered, and the camera flight behind goto (Workspace.tsx `onGoto`, tic-bee0) is the "teleport" -- a double-click on a line can reuse that flight to centre the line's `to` endpoint. What is missing: visually distinguishing the single nearest line under the cursor when a node is not under the pointer, and a double-click gesture on that line triggering the flight.

Acceptance criteria:
- Hovering empty canvas near a relationship line visually indicates the nearest line (constant screen-size pick radius; start from EDGE_HOVER_RADIUS_PX in web/src/settings.ts and tune), without disturbing node hover/selection behaviour.
- Over a bundle of near-overlapping lines, the nearest line is the one indicated; the existing near-pointer popup may still summarise the rest, but one line reads as "the one under the cursor".
- Double-clicking that line flies the camera to the line's target (its `to` endpoint), reusing the existing goto flight and its drag-override handling; an edge without resolvable endpoints no-ops.
- The nearest-line indication clears when the pointer moves away, over a node, or during pan/drag/marquee, and on any viewport change -- same gates as the tic-f1d7 popup.
- Frame rate stays smooth sweeping the pointer over the full ~190-file graph.

Classification note: reclassified from a raw `wish` to `feature` per the wishlist flow; filed in .arbite/wishlist/ until deliberately promoted to real work. Epic viz-modes-next because it is mode-agnostic scene-geometry interaction, the same surface as tic-f1d7.

## Notes
- 2026-09-02T11:39:25 deepseek.v4-flash.001: Implemented nearest-line hover highlight and double-click-to-fly. web/src/canvas/Workspace.tsx: probeEdges now records the nearest edge id (nearestEdgeId state) and its 'to' endpoint (nearestTargetRef); the nearest line draws in a new THEME.nearest colour, thicker, and is added to the edge ordering so it draws on top of the bundle. The near-pointer popup bolds its first line (the nearest) via .edge-popup-line-nearest. web/src/canvas/useViewport.ts: added onEmptyDoubleClick option and pure isEmptyDoubleClick() helper; two empty-canvas clicks inside DBLCLICK_MS (moved to settings.ts, shared with the node double-click) fire it, which emits the existing goto event to the nearest line's target (reusing the flight and drag-override handling; an edge without a resolvable 'to' no-ops). Clearing gates match the tic-f1d7 popup: pointer move over a node, pan/marquee/drag, pointer leave, and any viewport change all clear the nearest highlight and target. Tests: new useViewport.test.ts (4) for isEmptyDoubleClick, settings.test.ts DBLCLICK_MS check. 962/962 pass, tsc -b and vite build clean.

- 2026-09-02T11:50:31 deepseek.v4-flash.001: Reopened.

- 2026-09-02T11:53:13 deepseek.v4-flash.001: Follow-up fixes after a partial close. (1) The nearest line now animates like any highlighted connection: EdgeLine in web/src/canvas/Workspace.tsx passes highlighted || nearest to isAntsEdge, so a directional nearest line marches ants exactly as a lit line over a hovered/selected node does. (2) Double-click-to-fly was dead: useViewport set panning=true on the empty-canvas pointerdown, which flipped the Workspace suppressed gate and cleared the nearest highlight and its nearestTargetRef before the second click of a double-click released, so onEmptyDoubleClick read a null target and no-opped. Panning now means an ACTUAL pan: the flag flips only once the pointer crosses DRAG_THRESHOLD in move(), so a press (a potential double-click) keeps the highlight and target alive until the release, while a real pan still clears it the moment it starts. Clearing gates unchanged: node hover, pan/drag/marquee, pointer leave, viewport change. 962/962 pass; tsc -b and vite build clean.
