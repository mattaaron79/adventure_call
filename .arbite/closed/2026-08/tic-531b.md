---
id: tic-531b
title: 'Import graph: merge import lines into a junction system (elk mergeEdges +
  junction dots)'
status: closed
type: feature
tier: high
domain: ui
epic: viz-modes-next
priority: 12
tags:
- imports
- elk
- junctions
- mode-params
assignee: claude.opus.003
depends_on: []
blocked_by: null
created: '2026-08-31T20:27:53'
updated: '2026-08-31T21:01:48'
closed: '2026-08-31T21:01:48'
---

## Description
User request: in Import graph every line is currently separate; add a checkbox that merges them into a junction system, re-deriving the view when the value changes.

ELK already implements exactly this and it was confirmed against the ELK reference during planning: the layered option org.eclipse.elk.layered.mergeEdges (boolean, default false, applies to parents) makes all incoming edges to a node share one input port and all outgoing edges share one output port, so edges without explicit ports touch their nodes at unified points and overlap into shared trunks instead of fanning out. ELK then computes org.eclipse.elk.junctionPoints on each edge -- an OUTPUT property, not a user option, surfaced in the elkjs JSON result as edge.junctionPoints -- giving the points where junction symbols should be drawn. Do not hand-roll hyperedge geometry; use these two.

WORK
1. web/src/layout/elkTypes.ts: ElkLayoutOptions gains mergeEdges?: boolean. ElkGraphResult gains junctionPoints: ReadonlyMap<string, readonly {x,y}[]> (keyed by edge id).
2. web/src/layout/elkConvert.ts: toElkNode emits "elk.layered.mergeEdges": String(options.mergeEdges ?? false) alongside the existing layered options. fromElkResult collects each edges junctionPoints, offset by the accumulated ancestor origin exactly as the section start/bend/end points already are. elkjs ElkExtendedEdge does not declare junctionPoints -- narrow through a small local interface, not a bare any.
3. web/src/modes/types.ts: Positioned gains junctions?: readonly {x,y}[]; assemble() carries them onto the Scene. web/src/canvas/scene.ts: Scene gains junctions?, and cullScene filters them against the visible world rect like everything else.
4. web/src/canvas/Workspace.tsx: draw the junction dots as small non-listening Konva Circles in the existing edge layer (radius ~3 world px, THEME.edge). They are decoration, never hit targets.
5. web/src/modes/importGraph.ts: ImportGraphParams stops being Record<string, never> and becomes {mergeLines: boolean}, defaultParams {mergeLines: false}, with paramToggles [{key: "mergeLines", label: "Merge import lines"}]. ModePicker (web/src/ui/ModePicker.tsx) already renders a modes declared paramToggles as checkboxes generically, so there is NO new UI code. layout() passes {mergeEdges: params.mergeLines} through to layoutGraph.

THE CRUX -- do not skip. cacheKeyOf(spec) in importGraph.ts keys the single-slot elk layout cache on node ids and edge ids only. A param change alters no ids, so the toggle would hit the stale cache and appear to do nothing; the same trap will catch tic-ea9ds expanded containers, which change sizes without changing ids. Generalise it to cacheKeyOf(spec, sizes, params) -- fold in the measured sizes and the JSON-serialised params -- keeping it exported and unit-tested. The re-derive the user asked for then happens for free: a params change re-runs Apps renderMode useMemo, layout() misses the cache, kicks off elk, and notifyLayoutReady (web/src/modes/asyncLayout.ts) triggers the second, cache-hit render.

Verification: cd web && npm run test -- add tests in layout/elkConvert.test.ts (the option is emitted with both values; junction points are read and offset by the ancestor origin) and modes/importGraph.test.ts (the cache key varies with params and with sizes; the toggle is declared). npm run build. Then npm run dev, switch to Import graph, and toggle the checkbox: lines into a shared target should visibly collapse into common trunks with junction dots at the splits, and toggling back must restore the fan-out (proving the cache key change works).

## Notes
- 2026-08-31T21:01:48 claude.opus.003: Merged import lines into an elk junction system (tic-531b). ImportGraphParams stopped being Record<string, never> and became {mergeLines: boolean}, defaultParams {mergeLines: false}, with paramToggles [{key:'mergeLines', label:'Merge import lines'}] -- ModePicker's generic paramToggles rendering picked the checkbox up with zero new UI code, exactly as the ticket predicted. layout() now passes {mergeEdges: params.mergeLines} into layoutGraph; toElkNode emits 'elk.layered.mergeEdges' unconditionally (String(options.mergeEdges ?? false)) rather than only when true, so the option set has the same shape either way and there is no path where a stale merged routing survives because a key simply vanished. fromElkResult collects each edge's junctionPoints offset by the accumulated ancestor origin, in the same loop as the section start/bend/end points since they share that origin exactly. ElkGraphResult gained junctionPoints: ReadonlyMap<string, readonly ElkGraphPoint[]>; Positioned and Scene gained junctions?: readonly Point[]; assemble carries them through; cullScene point-tests them against the visible world rect. Workspace draws them as non-listening Konva Circles (radius 3 world px, THEME.edge, perfectDrawEnabled false) inside the existing edge layer, which already carries listening={false} -- so they can never be hit targets nor steal the empty-space drag that pans the camera.

THE CRUX, handled: cacheKeyOf(spec) became cacheKeyOf(spec, sizes, params), folding in each child's measured WxH (in children order) and JSON.stringify(params). Without it the toggle changes no node id and no edge id, so it would have hit the stale single-slot cache and done nothing in BOTH directions. Pinned by a test asserting merged != unmerged AND that toggling back reproduces the first key byte-for-byte (so the cached unmerged layout is reused rather than recomputed), plus a sizes test for the same trap tic-ea9d's expanded containers will hit.

TWO FINDINGS THAT CONTRADICT THE TICKET, both verified rather than assumed. (1) The ticket said elkjs's ElkExtendedEdge does not declare junctionPoints and instructed me to narrow through a local interface. It does declare it: ElkExtendedEdge extends the (deprecated) ElkEdge, which has junctionPoints?: ElkPoint[] -- see node_modules/elkjs/lib/elk-api.d.ts:55-59. So there is no cast, no local shim and no 'any'; edge.junctionPoints is read through elkjs's own type. Adding the planned interface would have been redundant code asserting something the library already asserts. (2) I initially documented the de-duplication in flattenJunctions as fixing elk reporting the same point on every edge through a trunk. Measurement says otherwise, so I corrected the docstrings rather than shipping a plausible-sounding false claim: a merged 42-edge fan-in produced 12 junction entries across 12 DISTINCT coordinates, i.e. elk attributes each junction to exactly one edge. The dedup stays, re-documented honestly as insurance (nothing in elk's contract promises one owner, and rounding to a whole world pixel also collapses near-coincident points the 3px dot would cover anyway), but it is not load-bearing today.

JUNCTION POINTS CONFIRMED REAL, not faked. No headless browser driver exists in this environment (no playwright/puppeteer in web/node_modules/.bin), so instead of eyeballing a canvas I drove elkjs directly and then drove it through my own conversion code, which is the stronger check. Against elk.bundled.js in Node on a 5-importers-into-one-hub fan-in: mergeEdges false gives 5 distinct hub entry points (316.0, 369.3, 342.7, 289.3, 262.7 at y=116) and ZERO junctionPoints on all 8 edges; mergeEdges true gives all 5 edges entering at the single point 92.0,116.0 and 4 junction coordinates at y=62 -- the trunk row one rank above the hub. Then a throwaway vitest file ran toElkNode -> real elk -> fromElkResult -> flattenJunctions end to end: unmerged junctionPoints.size 0 and flattenJunctions [] ; merged produced dots [{336,62},{204,62},{468,62}] with hub entries collapsing 5 -> 1. That file was deleted after use and is not in the commit -- the permanent tests keep the elk conversion layer pure and worker-free, which is the existing convention. So the merged routing genuinely looks like: the fan of independent diagonals into a popular module becomes one horizontal trunk sitting a rank above it, with dots where importers join that trunk, and the whole graph widens slightly (672 -> 692 px on the probe) because the trunk needs its own channel.

Deliberately NOT done: no hand-rolled hyperedge geometry (elk's two properties do it all); no junction dots surviving a drag -- reproject re-routes every import edge to a straight centre line between placed endpoints, so the dots would mark routing that no longer exists, and reproject drops them with a comment saying why; junctions are omitted rather than stored as an empty array when nothing merged, and cullScene uses scene.junctions?.filter so a scene with no junctions allocates nothing per pan frame, keeping the performance constraint intact; no new THEME colour; sceneBounds untouched since junctions lie on edges whose points already contribute. Scope respected: data/derive.ts, modes/fsTree.ts and modes/fileDetail.ts (tic-0680's) untouched.

Verification: 379 -> 391 tests, 23/23 files pass. 12 new: 3 in elkConvert.test.ts (mergeEdges emitted for both values; junction points read and offset by the ancestor origin through a nested container, 5,60 -> 1005,560; an edge with an empty junction array stays out of the map entirely), 2 in cull.test.ts (dots culled against the visible rect; junctions stays undefined not [] for a scene with none), 7 in importGraph.test.ts (params vary the cache key and toggling back restores it; sizes vary it; 3 flattenJunctions cases; defaultParams and paramToggles declared). One pre-existing assertion updated, not deleted: importGraphMode metadata asserted defaultParams equals {} under the old no-params design. npm run build (tsc -b + vite) clean, only the pre-existing chunk-size advisory. The tidyTree snapshot was not rewritten. Files changed: web/src/layout/elkTypes.ts, web/src/layout/elkConvert.ts, web/src/layout/elkConvert.test.ts, web/src/modes/types.ts, web/src/modes/importGraph.ts, web/src/modes/importGraph.test.ts, web/src/canvas/scene.ts, web/src/canvas/cull.test.ts, web/src/canvas/Workspace.tsx.
